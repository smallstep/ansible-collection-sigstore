name: Python Black

on:
  push:
    tags-ignore:
    - 'v*'
    branches:
    - "master"
  pull_request:

jobs:
  python-black:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: psf/black@stable
        with:
          options: "--check --verbose --color --diff"
